version: 2

models:
  - name: mrt_order_monthly_report
    description: 'this table contains information about monthly sell-out by product id, brand & products'
    columns:
      - name: product_id
        description: "Primary key, unique identifier for each product."
        tests:
          - unique
          - not_null