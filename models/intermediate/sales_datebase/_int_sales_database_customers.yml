version: 2

models:
  - name: int_sales_database__customers
    description: '{{ doc("int_sales_database__customers") }}'
    columns:
      - name: customer_id
        description: "Primary key, unique identifier for each customer."
        tests:
          - unique
          - not_null