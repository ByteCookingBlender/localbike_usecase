version: 2

models:
  - name: int_production_database__stock_value_by_store
    description: '{{ doc("int_production_database__stock_value_by_store") }}'
    columns:
      - name: store_id
        description: "Primary key, unique identifier for each store."
        tests:
          - unique
          - not_null